@model ICollection<Floriculture.Models.Plant>

@{
    ViewData["Title"] = "Painel de Controle";
}

<div class="container mt-4">

    <div class="content-header">
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="page-title">@Model.Count Plantas Monitoradas</h1>
            <div>
                <a class="btn btn-primary-custom shadow-sm" asp-action="Create">
                    <i class="fas fa-plus btn-icon">Cadastrar Nova Planta</i> 
                </a>
            </div>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover table-striped">
                        <thead class="text-uppercase">
                            <tr>
                                <th>Nome da Planta</th>
                                <th>Valor do Sensor</th>
                                <th>Último Evento</th>
                                <th class="text-center">Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var p in Model)
                            {
                                <tr>
                                    <td><strong>@p.Name</strong></td>
                                    <td>@p.SensorValue</td>
                                    <td>@p.SensorEvent</td>
                                    <td class="text-center">
                                        <div class="d-flex justify-content-center gap-2">

                                            <form asp-controller="Home" asp-action="Delete" method="post" onsubmit="return confirm('Tem certeza?');">
                                                <input type="hidden" name="id" value="@p.ID" />
                                                <button type="submit" class="btn btn-danger btn-sm btn-equal-width">Excluir</button>
                                            </form>

                                            <a class="btn btn-sm btn-outline-success btn-equal-width" asp-action="Update" asp-route-id="@p.ID">Edit</a>

                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center p-5">
                    <h4>Nenhuma planta cadastrada.</h4>
                    <p>Clique em "Cadastrar Nova Planta" para começar.</p>
                </div>
            }
        </div>
    </div>
</div>