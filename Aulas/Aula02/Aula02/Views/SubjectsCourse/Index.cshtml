@model ICollection<Aula02.Models.Course>

@{
    ViewData["Title"] = "Cursos/Matéria";
}

<div class="text-center">
    <h1 class="display-4">@Model.Count matérias por cursos.</h1>

    <a asp-action="Create" asp-controller="SubjectsCourse">Adicionar</a>

    <table class="table table-responsive table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var s in Model)
            {
                <tr>
                    <td>@s.ID</td>
                    <td>@s.Name</td>
                    <td>
                        <a asp-controller="SubjectsCourse" asp-action="Update" asp-route-studentId="@s.ID" method="get">
                            Editar
                        </a>
                    </td>
                </tr>

                @if(s.SubjectsCourse!.Count > 0)
                {

                    <tr>
                        <td colspan="5">
                            <ul>
                                @foreach (var sc in s.SubjectsCourse!)
                                    {
                                    <li>
                                        - @sc.Subject?.Name - 
                                            <form asp-controller="SubjectsCourse" asp-action="Delete" method="post">
                                                <input type="hidden" name="studentId" value="@sc.CourseID" />
                                                <input type="hidden" name="subjectId" value="@sc.SubjectID" />
                                                <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
                                            </form>
                                    </li>
                                    }
                            </ul>
                        </td>
                    </tr>
                }
            }
        </tbody>
</table>
</div>
